#include <bits/stdc++.h>

#ifndef ONLINE_JUDGE
#include "debug.h"
#endif

using namespace std;

#define all(X) X.begin(), X.end()


int main() {
    ios_base::sync_with_stdio(false);

    int v, t;

    while(cin >> v >> t) {
        cout << v * t * 2 << '\n';
    }

    return 0;
}
